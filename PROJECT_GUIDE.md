# 自动化度量工具项目文档

## 1. 项目概述

本项目用于完成《自动化度量工具设计》课程作业，目标是构建一个中小型软件度量自动化工具，对软件设计阶段和代码实现阶段的多个软件实体进行度量分析，并给出工作量、周期、成本和人员估算结果。

当前项目结构：

- 后端：`D:\software-metrics-analyzer\metrics`
- 前端：`D:\metrics-client\metrics-web`

系统当前支持：

- 基于 Java AST 的代码分析
- 基于真实 `.java` 文件的上传分析
- CK 模型指标分析
- LK 与传统指标分析
- 设计阶段特征输入
- 工作量、开发周期、成本、建议人力估算
- 风险提示和类级明细展示

## 2. 系统整体流程

系统分析流程如下：

1. 用户输入设计阶段信息
2. 用户输入或粘贴 Java 源代码，或上传一个/多个 Java 文件
3. 后端基于 AST 解析代码结构
4. 统计 CK、LK、LoC、复杂度等指标
5. 将设计阶段输入与代码阶段指标结合
6. 计算工作量、周期、成本和建议人力
7. 前端展示总览、指标表、风险信号和类级明细

## 3. 前端输入项说明

### 3.1 设计阶段特征

#### 参与者数

含义：用例图中的 Actor 数量，即与系统交互的外部角色数量。

示例：

- 学生
- 教师
- 管理员

若系统包含这 3 类角色，则参与者数为 `3`。

作用：反映需求参与对象数量，参与用例规模估算。

#### 用例数

含义：系统主要功能场景数量，即用例图中的 Use Case 数量。

示例：

- 登录
- 查询成绩
- 录入成绩
- 导出报表

若共 4 个功能，则用例数为 `4`。

作用：反映系统功能规模，参与用例点和工作量估算。

#### 类图类数

含义：设计阶段类图中的类数量。

作用：反映面向对象结构规模，参与设计规模估算。

#### 子类数

含义：设计阶段类图中的子类数量。

作用：与 CK 模型中的 `NOC` 对应，反映继承和复用情况。

#### 流程判定数

含义：流程图中的判断节点数量，例如 `if`、`switch`、循环条件等。

作用：用于和代码实际圈复杂度比较，判断设计与实现是否一致。

#### 事务数

含义：系统中的业务处理单元数量，可理解为相对独立的业务动作数。

示例：

- 借书
- 还书
- 查询图书
- 预约图书

作用：反映业务处理规模，参与用例点和工作量估算。

#### 实体数

含义：系统中核心业务对象或数据对象的数量。

示例：

- 学生
- 课程
- 教师
- 成绩

作用：反映业务建模规模，参与用例点和工作量估算。

### 3.2 估算参数

#### 团队人数

含义：项目预计参与开发的人数。

作用：作为估算参考，并和系统推荐人力进行对比。

#### 人月成本

含义：1 个人工作 1 个月的成本。

示例：`15000` 表示 `1 人月 = 15000 元`。

作用：用于计算项目总成本。

#### 生产率系数

含义：团队开发效率系数。

参考理解：

- `1.0`：正常效率
- `> 1.0`：效率较高
- `< 1.0`：效率较低

作用：效率越高，估算工作量越低。

## 4. 设计阶段度量结果说明

### 用例点

含义：根据设计阶段输入估算的功能规模值。

当前简化计算方式：

```text
用例规模 = 参与者数 × 1.5 + 用例数 × 2.0 + 事务数 × 0.8
用例点 = 用例规模 + 实体数 × 0.6
```

说明：数值越大，表示功能规模越大。

### 设计类数

含义：系统设计阶段的类规模。

当前取值逻辑：

```text
设计类数 = max(前端输入的类图类数, 代码分析识别出的类数量)
```

说明：这样可以同时兼顾设计图和实际代码实现。

### 事务数

含义：业务处理单元数量。

当前来源：直接采用用户输入值。

用途：参与用例规模和工作量估算。

### 一致性判断

含义：设计阶段复杂度和代码实现复杂度是否一致。

比较对象：

- 前端输入的 `流程判定数`
- 后端分析得到的 `圈复杂度`

判断规则：

```text
差值 = |流程判定数 - 圈复杂度|
差值 <= 2   -> 高度一致
差值 <= 5   -> 基本一致
差值 > 5    -> 偏差明显
```

## 5. 工作量与估算结果说明

### 5.1 工作量

界面显示项：`Effort`

单位：人月

含义：完成该项目大约需要多少人工作多少个月。

当前计算方式：

```text
adjustedKloc = max(LoC / 1000, 0.1)
基础工作量 = 2.4 × adjustedKloc^1.05
用例规模 = 参与者数 × 1.5 + 用例数 × 2.0 + 事务数 × 0.8
设计规模 = 类数 × 0.5 + 实体数 × 0.4 + 判定数 × 0.7
复杂度系数 = 1 + 圈复杂度 / 25 + CBO / 20

工作量 =
(
  基础工作量
  + 用例规模 × 0.15
  + 设计规模 × 0.08
)
× 复杂度系数
/ 生产率系数
```

### 5.2 开发周期

界面显示项：`Time`

单位：月

当前计算方式：

```text
开发周期 = 2.5 × 工作量^0.38
```

含义：根据工作量推导出的项目预计完成时间。

### 5.3 项目成本

界面显示项：`Cost`

当前计算方式：

```text
项目成本 = 工作量 × 人月成本
```

含义：根据人月成本估算项目总成本。

### 5.4 建议人力

界面显示项：`People`

当前计算方式：

```text
建议人力 = ceil(工作量 / 开发周期)
```

含义：按当前估算结果，项目平均需要投入的人数。

## 6. CK 模型指标说明

### WMC

全称：`Weighted Methods per Class`

含义：类的加权方法数，用于反映类的方法规模和行为复杂程度。

当前实现中：

```text
WMC ≈ 类中的方法数量
```

说明：WMC 越大，类承担的行为通常越多。

### DIT

全称：`Depth of Inheritance Tree`

含义：继承树深度。

当前实现中：

```text
DIT = 当前类到最上层父类的继承层数
```

### CBO

全称：`Coupling Between Object Classes`

含义：类与其他类之间的耦合程度。

当前实现中，若类中出现以下依赖，则统计相关类型：

- 字段类型
- 参数类型
- 返回值类型
- 继承类型
- 实现接口
- 对象创建

```text
CBO = 当前类依赖的其他类的数量
```

### RFC

全称：`Response For a Class`

含义：类的响应集合规模。

当前实现中主要统计：

- 类自身声明的方法
- 方法内部调用的方法

```text
RFC = 类声明方法集合 + 其调用方法集合的规模
```

### LCOM

全称：`Lack of Cohesion in Methods`

含义：类方法间缺乏内聚性的程度。

当前实现依据：

- 类有哪些字段
- 方法访问了哪些字段

系统使用简化方式评估：

```text
LCOM 越大，表示类内聚性越差
```

### NOC

全称：`Number of Children`

含义：当前类拥有的直接子类数量。

```text
NOC = 直接继承当前类的子类数
```

## 7. LK 与传统指标说明

### NOA

含义：属性数量。

```text
NOA = 类中字段总数
```

### NPM

含义：公开方法数量。

```text
NPM = public 方法数量
```

### NIV

含义：实例变量数量。

```text
NIV = 非 static 字段数量
```

### NVO

含义：重写方法数量。

```text
NVO = 带 @Override 的方法数量
```

### 类数量

含义：代码中识别出的类总数。

```text
类数量 = AST 识别出的 TypeDeclaration 数量
```

### 方法数量

含义：所有类中的方法总数。

```text
方法数量 = 所有类的方法数量之和
```

### 圈复杂度

英文：`Cyclomatic Complexity`

含义：代码分支和控制流程复杂程度。

当前规则：

- 每个方法初始复杂度为 `1`
- 每出现一个分支控制点，复杂度加 `1`

当前统计的典型控制点包括：

- `if`
- `for`
- `enhanced for`
- `while`
- `do while`
- `catch`
- `switch`
- `case`
- `?:`
- `&&`
- `||`

```text
方法圈复杂度 = 1 + 分支控制点数量
总圈复杂度 = 所有方法圈复杂度之和
```

### 平均复杂度

含义：平均每个方法的复杂度。

```text
平均复杂度 = 总圈复杂度 / 方法数量
```

## 8. 测试流程

### 8.1 环境准备

本项目后端必须使用 `Java 17+`。当前机器默认 JDK 可能仍为 JDK 8，启动前建议先设置：

```powershell
$env:JAVA_HOME='D:\java'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
java -version
```

若输出为 `21.0.1` 或其他 `17+` 版本即可。

### 8.2 后端测试

进入后端目录：

```powershell
cd D:\software-metrics-analyzer\metrics
```

运行单元测试：

```powershell
.\mvnw.cmd test
```

作用：

- 验证 Spring Boot 项目可正常启动
- 验证分析服务可正常返回结构化度量结果

启动后端服务：

```powershell
.\mvnw.cmd spring-boot:run
```

接口地址：

```text
http://localhost:8080/api/analyze
```

### 8.3 前端测试

进入前端目录：

```powershell
cd D:\metrics-client\metrics-web
```

构建检查：

```powershell
npm.cmd run build
```

本地开发启动：

```powershell
npm.cmd run dev
```

一般访问地址为：

```text
http://localhost:5173
```

### 8.4 联调测试

联调步骤建议如下：

1. 启动后端 Spring Boot 服务
2. 启动前端 Vite 服务
3. 打开前端页面
4. 选择输入模式
5. 若为“代码粘贴”，点击“载入示例”并检查代码
6. 若为“文件上传”，选择一个或多个 `.java` 文件
7. 检查设计阶段输入是否已填入或已手工设置
8. 点击“执行度量分析”
9. 观察右侧结果是否正常显示

重点检查：

- 工作量、周期、成本、建议人力是否正常显示
- CK 指标是否返回
- LK 和传统指标是否返回
- 设计阶段度量是否返回
- 风险信号是否返回
- 类级明细是否返回
- 多文件上传时是否显示上传摘要和文件名列表

### 8.5 文件上传分析说明

当前文件上传能力为真正的后端上传分析流程，而不是仅由前端读取文件内容后转成文本提交。

流程如下：

1. 前端通过 `multipart/form-data` 上传一个或多个 `.java` 文件
2. 同时上传设计阶段参数和估算参数
3. 后端接口 `/api/analyze/upload` 接收文件流和表单字段
4. 后端逐个读取文件内容并执行 AST 分析
5. 对多文件分析结果做聚合汇总
6. 返回项目级总览、类级明细和上传摘要

适用场景：

- 单文件类分析
- 多文件课程作业演示
- 小型 Java 项目模块分析

### 8.6 指标变化验证

可用以下方式验证系统是否计算正确：

1. 增加 `if / for / while`
   - 圈复杂度上升
   - WMC 和平均复杂度可能上升

2. 增加字段
   - NOA、NIV 上升

3. 增加 public 方法
   - NPM 上升

4. 引入更多外部类依赖
   - CBO、RFC 上升

5. 修改流程判定数
   - 一致性判断结果变化

## 9. 后续开发建议

当前版本已经满足课程演示和实验报告编写需要，后续可继续扩展：

1. 支持上传 Java 文件或项目目录，而不只是粘贴代码
2. 支持多文件分析和项目级汇总
3. 支持更严格的 LCOM 计算公式
4. 支持功能点、特征点等更多度量模型
5. 支持导出实验报告或 PDF
6. 支持图表展示，例如趋势图、雷达图、柱状图
7. 支持上传压缩包并自动解压扫描
8. 支持更多语言，如 C++、Python

## 10. 答辩时可直接使用的总结

本系统结合课程要求，从设计阶段和实现阶段两个层面进行软件度量。设计阶段通过参与者数、用例数、类图类数、流程判定数、事务数和实体数对功能规模和结构规模进行描述；实现阶段通过 Java AST 自动提取 CK 模型、LK 指标、代码规模和复杂度指标；最后将这些结果综合用于工作量、开发周期、成本和建议人力估算，从而实现对软件项目规模、复杂性、可维护性和开发投入的自动化分析。
